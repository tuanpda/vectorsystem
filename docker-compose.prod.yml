# Overlay tùy chọn (tương thích deploy/compose-prod.sh).
# Port localhost + MinIO cpuv1 đã cấu hình trong docker-compose.yml
# (tránh !reset — một số bản Docker Compose trên VPS xóa hết ports).

services: {}
